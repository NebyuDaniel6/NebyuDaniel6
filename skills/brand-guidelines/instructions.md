# Brand guidelines

When the user says “never do X”, store `explicit_rule`. When they approve a layout, store `approved_concept`. When they reject, store `rejected_concept`. Campaign-only notes are `campaign_temporary`.
